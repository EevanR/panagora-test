import Vue from 'vue'

const Home = Vue.extend({
  name: 'Home',
  render () {
    return (
      <div>
        <h1>If i  up date this it should render</h1>
      </div>
    )
  }
})

export default Home
